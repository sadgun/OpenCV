Face detection using OpenCV. 

Usage:

Comiple the main.cpp file and run the FaceDetection.exe file with the camera number as arguement( default camera number is 0).

Make sure that the XML classifier files are placed in the build folder else modify the path of the  XML files in the main.cpp file.


CMake was used to compile and run the program.
